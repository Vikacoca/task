let number1 = +prompt("Enter the first number:");
let number2 = +prompt("Enter the secong number:");
let F = 3 * Math.pow((number1 + number2), 3) + 275 * Math.pow(number2, 2) - 127 * number1 - 41;
console.log(F);